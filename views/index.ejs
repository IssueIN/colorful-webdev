<h2>Recently Added</h2>
<div>
  <% productsRecent.forEach(product => { %>
    <div><%= product.partNumber %></div>
  <% }) %>
</div>

<h2>Manufacturers</h2>
<div>
  <% manufacturers.forEach(manufacturer => { %>
    <div><%= manufacturer.name%></div>
  <% }) %>
</div>
