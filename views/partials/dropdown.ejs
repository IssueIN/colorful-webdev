<ul class="dropdown">
  <% items.forEach(item => { %>
    <% if (item.name) { %>
      <li><a><%= item.name %></a></li>
    <% } else { %>
      <li><a href="/products/<%= item %>"><%= item %></a></li>
    <% } %>
  <% }) %>
</ul>